% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster.R
\name{sw_cluster_run}
\alias{sw_cluster_run}
\title{Cluster Cells Using kohonen SOM or FastPG}
\usage{
sw_cluster_run(
  corrected,
  lineage_markers,
  method = c("som", "fastpg"),
  xdim = 10,
  ydim = 10,
  rlen = 10,
  n_metaclusters = 20,
  k = 30,
  seed = 42,
  ...
)
}
\arguments{
\item{corrected}{A \code{tibble}, \code{data.frame}, or numeric matrix
containing the (typically batch-corrected) expression data.}

\item{lineage_markers}{Character vector of marker names to use for
clustering. These should be lineage markers (e.g., CD3, CD4, CD8), not
functional markers.}

\item{method}{Character; clustering method to use. One of \code{"som"}
(default) for kohonen SOM with hierarchical metaclustering, or
\code{"fastpg"} for FastPG graph-based clustering.}

\item{xdim}{Integer; SOM grid x-dimension (default: 10). Used only when
\code{method = "som"}.}

\item{ydim}{Integer; SOM grid y-dimension (default: 10). Used only when
\code{method = "som"}.}

\item{rlen}{Integer; number of times the complete dataset is presented to
the SOM network (default: 10). Used only when \code{method = "som"}.}

\item{n_metaclusters}{Integer; number of metaclusters to identify
(default: 20). Used only when \code{method = "som"}.}

\item{k}{Integer; number of nearest neighbours for the FastPG shared
nearest-neighbour graph (default: 30). Used only when
\code{method = "fastpg"}.}

\item{seed}{Integer; random seed for reproducibility (default: 42).}

\item{...}{Additional arguments passed to the underlying clustering
function (\code{kohonen::som} or \code{FastPG::fastCluster}).}
}
\value{
An \code{sw_cluster_result} list with components:
\describe{
\item{\code{assignments}}{Integer vector of cluster/metacluster labels
(one per cell).}
\item{\code{data}}{The numeric matrix of expression values used for
clustering.}
\item{\code{lineage_markers}}{Character vector of marker names.}
\item{\code{method}}{The clustering method used.}
\item{\code{model}}{The underlying model object (\code{kohonen} SOM
object for \code{"som"}, or \code{NULL} for \code{"fastpg"}).}
\item{\code{n_clusters}}{Integer; total number of clusters found.}
}
}
\description{
Clusters cells from expression data using either a self-organizing map
(SOM) via the \pkg{kohonen} package with hierarchical metaclustering, or
fast graph-based community detection via the \pkg{FastPG} package.
}
\examples{
\dontrun{
df <- data.frame(
  CD3 = c(rnorm(100, 1), rnorm(100, 5)),
  CD4 = c(rnorm(100, 2), rnorm(100, 6))
)
result <- sw_cluster_run(df, lineage_markers = c("CD3", "CD4"),
                     method = "som", xdim = 5, ydim = 5,
                     n_metaclusters = 5)
table(sw_cluster_assignments(result))
}

}
