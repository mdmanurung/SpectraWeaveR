% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dimred.R
\name{sw_dimred_run}
\alias{sw_dimred_run}
\title{Run Dimensionality Reduction}
\usage{
sw_dimred_run(
  df,
  markers,
  method = c("umap", "pca"),
  n_dims = 2L,
  n_neighbors = 15L,
  min_dist = 0.2,
  max_cells = 50000L,
  scale = TRUE,
  seed = 42L,
  ...
)
}
\arguments{
\item{df}{A \code{data.frame} or \code{tibble} containing marker columns
and optional metadata columns (e.g., batch, sample, cluster).}

\item{markers}{Character vector of marker column names to include in
the reduction.}

\item{method}{Character; dimensionality reduction method.
One of \code{"umap"} (default) or \code{"pca"}.}

\item{n_dims}{Integer; number of output dimensions (default: 2).}

\item{n_neighbors}{Integer; number of neighbours for UMAP
(default: 15).  Ignored for PCA.}

\item{min_dist}{Numeric; minimum distance parameter for UMAP
(default: 0.2).  Ignored for PCA.}

\item{max_cells}{Integer or \code{NULL}; if the input has more rows
than \code{max_cells}, a random subsample is drawn before computing
the reduction (default: 50000).  Set \code{NULL} to disable.}

\item{scale}{Logical; whether to centre and scale columns before
reduction (default: \code{TRUE}).}

\item{seed}{Integer; random seed for reproducibility (default: 42).}

\item{...}{Additional arguments passed to \code{uwot::umap()} or
\code{stats::prcomp()}.}
}
\value{
A \code{tibble} with columns \code{dim1}, \code{dim2}
(and \code{dim3}, etc. when \code{n_dims > 2}), plus all non-marker
columns from the input.  An attribute \code{"method"} records the
method used, and for PCA an attribute \code{"variance_explained"}
stores the proportion of variance explained per component.
}
\description{
Computes a 2D embedding of marker expression data using UMAP or PCA.
Returns a tibble with coordinates and all non-marker metadata columns
from the input preserved.
}
\examples{
\dontrun{
df <- data.frame(CD3 = rnorm(200), CD4 = rnorm(200), batch = "B1")
# PCA (no extra dependency)
result <- sw_dimred_run(df, markers = c("CD3", "CD4"), method = "pca")
head(result)
# UMAP (requires uwot)
result <- sw_dimred_run(df, markers = c("CD3", "CD4"), method = "umap")
}

}
\seealso{
\code{\link{sw_plot_dimred}},
\code{\link{sw_plot_batch_dimred}}
}
