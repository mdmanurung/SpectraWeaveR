% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/targets_backend.R
\name{targets_backend}
\alias{targets_backend}
\title{\code{targets} Execution Backend for Composable Pipelines}
\description{
Optional execution backend that runs a \code{\link[=sw_pipeline]{sw_pipeline()}} through
the \href{https://docs.ropensci.org/targets/}{\code{targets}} package. Each
\code{\link[=sw_pipeline_step]{sw_pipeline_step()}} in the pipeline is translated to a single
\code{targets::tar_target()} so that:
\itemize{
\item reruns automatically skip steps whose definitions have not changed,
\item expensive steps can be parallelised through \code{targets}' built-in
workers,
\item the user does not have to write or maintain a \verb{_targets.R} script
by hand.
}

Two entry points are provided:
\itemize{
\item \code{\link[=sw_pipeline_run_targets]{sw_pipeline_run_targets()}} — generate the script and immediately
run it via \code{targets::tar_make()}, returning the same shape as
\code{\link[=sw_pipeline_run]{sw_pipeline_run()}}.
\item \code{\link[=sw_pipeline_to_targets]{sw_pipeline_to_targets()}} — generate the script and the
supporting RDS files, but do \strong{not} run anything. Useful for
inspection, hand-editing, or driving \code{targets} yourself.
}

The classic sequential backend is unchanged and remains the default; the
new backend is opt-in via \code{sw_pipeline_run(..., backend = "targets")}.
}
\section{Caveats}{

\itemize{
\item Step functions and their captured environments must be
\code{serialize()}-able. Functions exported from R packages (the common
case via \verb{sw_step_*()} constructors) work transparently because their
enclosing environment is the package namespace.
\item Step names are sanitised to valid R symbols via \code{make.names()}
before being used as \code{tar_target} names; the original names remain the
keys of the returned \code{intermediates} list.
}
}

\keyword{internal}
