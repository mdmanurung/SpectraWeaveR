% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch_correct.R
\name{sw_correct_evaluate_mad}
\alias{sw_correct_evaluate_mad}
\title{Evaluate Batch Correction with MAD}
\usage{
sw_correct_evaluate_mad(
  uncorrected,
  corrected,
  markers,
  cell_col = "label",
  batch_col = "batch",
  ...
)
}
\arguments{
\item{uncorrected}{A \code{tibble} with pre-correction data (must include
\code{batch} and \code{label} columns).}

\item{corrected}{A \code{tibble} with post-correction data (must include
\code{batch} and \code{label} columns).}

\item{markers}{Character vector of marker column names to evaluate.}

\item{cell_col}{Name of the cluster label column (default: \code{"label"}).}

\item{batch_col}{Name of the batch column (default: \code{"batch"}).}

\item{...}{Additional arguments passed to \code{cyCombine::evaluate_mad()}.}
}
\value{
A named list as returned by \code{cyCombine::evaluate_mad()},
typically containing:
\describe{
\item{\code{score}}{Overall MAD score}
\item{\code{mad}}{A \code{tibble} with per-marker MAD values}
}
}
\description{
Computes Median Absolute Deviation (MAD) of batch medians within SOM
clusters, before and after correction.  Both data frames must have a
\code{label} column with matching cluster assignments.
}
\seealso{
\code{\link{sw_correct_evaluate_emd}}, \code{\link{sw_correct_evaluate_quick}}
}
