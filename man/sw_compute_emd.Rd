% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch_correct.R
\name{sw_compute_emd}
\alias{sw_compute_emd}
\title{Compute Earth Mover's Distance}
\usage{
sw_compute_emd(
  df,
  markers,
  cell_col = "label",
  batch_col = "batch",
  binSize = 0.1,
  ...
)
}
\arguments{
\item{df}{A \code{tibble} with marker columns, \code{batch}, and
\code{label} columns.}

\item{markers}{Character vector of marker column names.}

\item{cell_col}{Name of the cluster label column (default: \code{"label"}).}

\item{batch_col}{Name of the batch column (default: \code{"batch"}).}

\item{binSize}{Numeric; bin size for EMD discretisation (default: 0.1).}

\item{...}{Additional arguments passed to \code{cyCombine::compute_emd()}.}
}
\value{
A named list of EMD matrices (per cluster, per marker), as returned
by \code{cyCombine::compute_emd()}.
}
\description{
Computes Earth Mover's Distance (EMD) between batch distributions for each
marker within each SOM cluster.  The data must contain a \code{label}
column (cluster assignments); use \code{\link{sw_create_som}} to create
one if needed.
}
\seealso{
\code{\link{sw_evaluate_emd}}, \code{\link{sw_evaluate_mad}}
}
