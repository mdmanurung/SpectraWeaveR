% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc.R
\name{sw_qc_run}
\alias{sw_qc_run}
\title{Signal Quality Control on a Single flowFrame}
\usage{
sw_qc_run(
  ff,
  channels = NULL,
  IT_limit = 0.55,
  MAD = 6,
  output_dir = NULL,
  ...
)
}
\arguments{
\item{ff}{A \code{flowFrame} object (should be transformed before QC).}

\item{channels}{Character vector of channel names to evaluate. If
\code{NULL}, all fluorescent channels (excluding FSC/SSC/Time) are used.}

\item{IT_limit}{Numeric; Isolation Tree contamination threshold
(default: 0.55). Lower values are more aggressive in removing events.}

\item{MAD}{Numeric; Median Absolute Deviation threshold (default: 6).
Lower values are more aggressive.}

\item{output_dir}{Character path for QC report output. If \code{NULL},
no plots are generated.}

\item{...}{Additional arguments passed to \code{PeacoQC::PeacoQC()}.}
}
\value{
A list with components:
\describe{
\item{\code{FinalFF}}{The cleaned \code{flowFrame} with aberrant events
removed}
\item{\code{GoodCells}}{Logical vector indicating retained (TRUE) vs
removed (FALSE) events}
\item{\code{n_removed}}{Number of events removed}
\item{\code{pct_removed}}{Percentage of events removed}
}
}
\description{
Wrapper for \code{\link[PeacoQC]{PeacoQC}} with sensible defaults for
spectral flow cytometry data. Identifies and removes events with aberrant
signal behavior using Isolation Trees and MAD-based methods.
}
\examples{
\dontrun{
if (requireNamespace("flowCore", quietly = TRUE)) {
  mat <- matrix(abs(rnorm(2000, 50000, 15000)), ncol = 4,
                dimnames = list(NULL, c("FSC-A", "SSC-A", "BV421-A", "PE-A")))
  ff <- flowCore::flowFrame(mat)
  result <- sw_qc_run(ff, channels = c("BV421-A", "PE-A"))
  cat("Removed:", result$n_removed, "events\n")
}
}

}
