% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch_correct.R
\name{sw_correct_run}
\alias{sw_correct_run}
\title{Batch Correct Expression Data}
\usage{
sw_correct_run(
  uncorrected,
  markers,
  covar = NULL,
  label = NULL,
  xdim = 8,
  ydim = 8,
  rlen = 10,
  norm_method = "scale",
  seed = 42,
  ...
)
}
\arguments{
\item{uncorrected}{A \code{tibble} as produced by
\code{\link{sw_correct_prepare}}.}

\item{markers}{Character vector of marker column names to correct.}

\item{covar}{Character scalar naming the covariate column (typically
\code{"condition"} or \code{NULL}).}

\item{label}{Optional pre-computed cluster labels (integer vector, same
length as \code{nrow(uncorrected)}).  When supplied the internal SOM step
is skipped.}

\item{xdim}{Integer; SOM grid x-dimension (default: 8).}

\item{ydim}{Integer; SOM grid y-dimension (default: 8).}

\item{rlen}{Integer; SOM training length (default: 10).}

\item{norm_method}{Character; normalization method for cyCombine
(default: \code{"scale"}).  One of \code{"scale"}, \code{"rank"},
\code{"none"}.}

\item{seed}{Integer; random seed for reproducibility (default: 42).}

\item{...}{Additional arguments passed to \code{cyCombine::batch_correct()}.}
}
\value{
A \code{tibble} with batch-corrected marker values.
}
\description{
All-in-one wrapper for \code{cyCombine::batch_correct()} that normalises,
clusters with a Self-Organising Map, and applies ComBat-based batch
correction.  For finer control use the modular functions
\code{\link{sw_correct_normalize}}, \code{\link{sw_correct_som}}, and
\code{\link{sw_correct_apply}}.
}
\examples{
\dontrun{
uncorrected <- tibble::tibble(
  CD3 = c(rnorm(100, 1), rnorm(100, 3)),
  CD4 = c(rnorm(100, 2), rnorm(100, 4)),
  batch = rep(c("B1", "B2"), each = 100),
  sample = rep(c("S1", "S2"), each = 100)
)
corrected <- sw_correct_run(uncorrected, markers = c("CD3", "CD4"))
}

}
