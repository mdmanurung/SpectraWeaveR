% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{sw_io_exprs_to_tibble}
\alias{sw_io_exprs_to_tibble}
\title{Convert Expression Matrix to Tibble}
\usage{
sw_io_exprs_to_tibble(
  mat,
  colnames = NULL,
  sample = NULL,
  batch = NULL,
  condition = NULL
)
}
\arguments{
\item{mat}{A numeric matrix with cells as rows and channels as columns.}

\item{colnames}{Optional character vector of column names. If \code{NULL},
uses \code{colnames(mat)}.}

\item{sample}{Character scalar identifying the sample (default: \code{NULL}).}

\item{batch}{Character or integer scalar identifying the batch
(default: \code{NULL}).}

\item{condition}{Character scalar identifying the experimental condition
(default: \code{NULL}).}
}
\value{
A \code{tibble}.
}
\description{
Converts a numeric matrix to a tibble with optional metadata columns.
Useful for converting output from \code{flowCore::exprs()} or similar
matrix-returning functions.
}
\examples{
mat <- matrix(rnorm(20), ncol = 2, dimnames = list(NULL, c("CD3", "CD4")))
sw_io_exprs_to_tibble(mat, sample = "S1", batch = "B1")

}
