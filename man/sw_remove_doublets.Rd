% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gating_utils.R
\name{sw_remove_doublets}
\alias{sw_remove_doublets}
\title{Remove Doublets Using CytoPipeline Algorithm}
\usage{
sw_remove_doublets(
  ff,
  area_channels = c("FSC-A", "SSC-A"),
  height_channels = c("FSC-H", "SSC-H"),
  nmads = rep(4, length(area_channels)),
  verbose = FALSE
)
}
\arguments{
\item{ff}{A \code{flowCore::flowFrame}.}

\item{area_channels}{Character vector of area-type channel names
(e.g., \code{c("FSC-A", "SSC-A")}). Length 1 or 2.}

\item{height_channels}{Character vector of height-type channel names
(e.g., \code{c("FSC-H", "SSC-H")}). Must match length of
\code{area_channels}.}

\item{nmads}{Numeric vector; MAD bandwidth per channel pair.
Default: \code{rep(4, length(area_channels))}.}

\item{verbose}{Logical; if \code{TRUE}, prints gate statistics.
Default: \code{FALSE}.}
}
\value{
A \code{flowFrame} with doublets removed.
}
\description{
Removes doublet events using the parallelogram singlet gate from
\code{\link{sw_singlets_gate}}. Can apply gates on multiple channel pairs
(e.g., FSC-A/FSC-H and SSC-A/SSC-H) and combine them.
}
\details{
Ported from CytoPipeline (UCLouvain-CBIO).
}
